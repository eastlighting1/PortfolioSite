---
interface Props {
  eyebrow?: string;
  title: string;
  description: string;
  meta?: string[];
  className?: string;
}

const { eyebrow, title, description, meta = [], className = "" } = Astro.props;
---

<section class={`page-hero ${className}`.trim()} data-reveal="1">
  {eyebrow && <div class="eyebrow">{eyebrow}</div>}
  <h1 class="section-title" set:html={title} />
  <p class="section-lead" set:html={description} />
  {meta.length > 0 && (
    <div class="meta-row">
      {meta.map((item) => (
        <span class="tag-chip">{item}</span>
      ))}
    </div>
  )}
</section>
