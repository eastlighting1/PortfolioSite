---
interface Props {
  title: string;
  className?: string;
}

const { title, className = "" } = Astro.props;
---

<section class={`resume-section ${className}`.trim()}>
  <h2>{title}</h2>
  <slot />
</section>
