---
import { getGlobalEntry } from "../../data/content";
import { type Locale } from "../../data/i18n";

interface Props {
  eyebrow: string;
  title: string;
  lead?: string;
  locale?: Locale;
}

const { eyebrow, title, lead, locale = "ko" } = Astro.props;
const profileEntry = await getGlobalEntry("profile", locale);
const profile = profileEntry.data;
---

<section class="section">
  <div class="container">
    <div class="eyebrow">{eyebrow}</div>
    <h2 class="section-title section-title--single-line">{title}</h2>
    {lead && <p class="section-lead">{lead}</p>}
    <div class="system-grid" style="margin-top: 2rem;">
      {profile.pipeline.map((stage, index) => (
        <article class="system-stage" data-reveal={`${(index % 4) + 1}`}>
          <span class="small-label">{stage.emphasis}</span>
          <h3>{stage.title}</h3>
          <p class="muted">{stage.description}</p>
        </article>
      ))}
    </div>
  </div>
</section>
