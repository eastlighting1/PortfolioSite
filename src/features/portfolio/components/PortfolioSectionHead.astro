---
const { eyebrow, title, lead } = Astro.props;
---

<div class="portfolio-section-head">
  <p class="portfolio-section-head__eyebrow">{eyebrow}</p>
  <h2>{title}</h2>
  {lead && <p>{lead}</p>}
</div>
